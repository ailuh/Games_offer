{
  "name": "watchlist-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Private group watchlist for games and videos, with a Telegram bot, AI post parsing, release notifications, and synchronized YouTube watch parties.",
  "license": "MIT",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev:api": "pnpm --filter @app/api dev",
    "dev:web": "pnpm --filter @app/web dev"
  }
}
