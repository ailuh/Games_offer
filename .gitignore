# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo
apps/web/dist/
apps/api/dist/

# env & secrets — ignore every .env variant (incl. stray "copy" files), keep the example
.env*
!.env.example

# local planning docs and editor/agent state (kept out of the public repo)
docs/
.claude/
**/CLAUDE.md
**/CLAUDE.local.md
.idea/
.vscode/

# prisma
apps/api/prisma/*.db

# local-only docker overrides (e.g. exposing Postgres for host dev)
docker-compose.override.yml

# logs & misc
*.log
npm-debug.log*
.DS_Store
coverage/

# scratch / temp scripts
._*
